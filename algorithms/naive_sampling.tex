
\begin{algorithm}[H]
    \caption{Algoritmo di banale}
    \DontPrintSemicolon
    $S = \emptyset$\;
    \BlankLine
    \For{tupla $t \in U$}{
        Scegli un \emph{bucket} $\in_u [10]$\;    
        Salva $t$ in $S$ se il \emph{bucket} è $0$\;

    }
    
    \For{query $q \in U$}{
        \KwRet la frazione delle \emph{occorrenze $q$} in $S$\;   
    }
\end{algorithm}
